<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>AI 平台账号密码清单（模板）</title>
<style>
  :root { color-scheme: dark; }
  * { box-sizing: border-box; }
  body {
    margin: 0; padding: 32px 20px; background: #0d1117; color: #c9d1d9;
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", "Microsoft YaHei", sans-serif;
    line-height: 1.6;
  }
  .wrap { max-width: 960px; margin: 0 auto; }
  h1 { font-size: 26px; color: #e6edf3; border-bottom: 1px solid #21262d; padding-bottom: 12px; }
  h2 { font-size: 19px; color: #58a6ff; margin-top: 32px; }
  .warn {
    background: rgba(248,81,73,0.12); border: 1px solid rgba(248,81,73,0.4);
    color: #ff7b72; padding: 12px 16px; border-radius: 8px; margin: 16px 0;
    font-weight: 600;
  }
  table { width: 100%; border-collapse: collapse; margin: 12px 0; font-size: 14px; }
  th, td { border: 1px solid #30363d; padding: 8px 12px; text-align: left; }
  th { background: #161b22; color: #e6edf3; }
  td { background: #0d1117; }
  code {
    background: #161b22; border: 1px solid #30363d; border-radius: 4px;
    padding: 1px 6px; font-family: "Consolas", "Monaco", monospace;
    color: #79c0ff; font-size: 13px;
  }
  .ph { color: #d29922; }   /* 占位符 */
  .note { color: #8b949e; font-size: 13px; margin-top: 24px; }
</style>
</head>
<body>
<div class="wrap">
  <h1>🔐 AI 平台账号密码清单（模板）</h1>
  <div class="warn">⚠️ 本文件是「模板」，所有 <span class="ph">黄色占位符</span> 都需你替换成真实值。<b>填写完后请妥善保管、切勿提交到代码仓库。</b></div>

  <p class="note">使用方法：部署过程中把你实际设置的账号/密码/密钥填进下面各表的占位符即可。占位符（如 <code>CHANGE_ME_STRONG_PASSWORD</code>）与 <code>.env.example</code> 里的变量一一对应。</p>

  <h2>一、统一管理员账号（核心）</h2>
  <table>
    <tr><th>项目</th><th>值</th></tr>
    <tr><td>统一用户名</td><td><code class="ph">你的管理员用户名</code></td></tr>
    <tr><td>统一密码（应用层）</td><td><code class="ph">CHANGE_ME_STRONG_PASSWORD</code></td></tr>
    <tr><td>管理员邮箱</td><td><code class="ph">admin@example.com</code></td></tr>
  </table>

  <h2>二、各平台管理员账号</h2>
  <table>
    <tr><th>平台</th><th>用户名</th><th>密码</th><th>说明</th></tr>
    <tr><td>Keycloak</td><td><code class="ph">你的管理员用户名</code></td><td><code class="ph">CHANGE_ME_STRONG_PASSWORD</code></td><td>Master Realm 管理员</td></tr>
    <tr><td>NewAPI</td><td><code class="ph">你的管理员用户名</code></td><td><code class="ph">CHANGE_ME_STRONG_PASSWORD</code></td><td>Root 管理员（role=100）</td></tr>
    <tr><td>AI 管理中心</td><td><code class="ph">你的管理员用户名</code></td><td><code class="ph">CHANGE_ME_STRONG_PASSWORD</code></td><td>Global Admin</td></tr>
    <tr><td>Ghost</td><td><code class="ph">你的管理员用户名</code></td><td><code class="ph">CHANGE_ME_STRONG_PASSWORD</code></td><td>首次访问向导时创建</td></tr>
    <tr><td>Gitea</td><td><code class="ph">你的管理员用户名</code></td><td><code class="ph">CHANGE_ME_STRONG_PASSWORD</code></td><td>安装向导时创建</td></tr>
    <tr><td>Dify</td><td><code class="ph">admin@example.com</code></td><td><code class="ph">CHANGE_ME_STRONG_PASSWORD</code></td><td>首次访问用邮箱创建</td></tr>
  </table>

  <h2>三、AD 域账号（LDAP 绑定）</h2>
  <table>
    <tr><th>项目</th><th>值</th></tr>
    <tr><td>sAMAccountName</td><td><code class="ph">你的管理员用户名</code></td></tr>
    <tr><td>显示名（CN，带空格）</td><td><code class="ph">你的显示名（可带空格）</code></td></tr>
    <tr><td>密码</td><td><code class="ph">CHANGE_ME_STRONG_PASSWORD</code></td></tr>
    <tr><td>Bind DN</td><td><code class="ph">CN=你的显示名,CN=Users,DC=company,DC=local</code></td></tr>
  </table>
  <p class="note">⚠️ LDAP 的 bindDn 用的是带空格的 CN「你的显示名」，不是下划线。</p>

  <h2>四、服务密钥与数据库密码</h2>
  <table>
    <tr><th>项目</th><th>值</th><th>说明</th></tr>
    <tr><td>NewAPI MySQL 密码</td><td><code class="ph">CHANGE_ME_STRONG_PASSWORD</code></td><td>NEWAPI_DB_PASSWORD</td></tr>
    <tr><td>NewAPI Session Secret</td><td><code class="ph">change-me-to-random-string</code></td><td>NEWAPI_SESSION_SECRET</td></tr>
    <tr><td>NewAPI Crypto Secret</td><td><code class="ph">change-me-to-random-string</code></td><td>NEWAPI_CRYPTO_SECRET</td></tr>
    <tr><td>LiteLLM Master Key</td><td><code class="ph">sk-litellm-CHANGE_ME</code></td><td>NewAPI 连接 LiteLLM 用</td></tr>
    <tr><td>DeepSeek API Key</td><td><code class="ph">sk-your-deepseek-key-here</code></td><td>外部 LLM 密钥</td></tr>
    <tr><td>AI 管理中心 Session Secret</td><td><code class="ph">change-me-to-random-string</code></td><td>SESSION_SECRET</td></tr>
    <tr><td>MCP 管理 API Token</td><td><code class="ph">change-me-mcp-admin-token</code></td><td>MCP_ADMIN_TOKEN</td></tr>
  </table>

  <h2>五、Keycloak OIDC Client Secret</h2>
  <table>
    <tr><th>Client</th><th>Secret</th><th>说明</th></tr>
    <tr><td>newapi</td><td><code class="ph">change-me-newapi-oidc-secret</code></td><td>NewAPI 接 Keycloak SSO</td></tr>
    <tr><td>AI-all-in-one-admin-portal</td><td><code class="ph">change-me-keycloak-client-secret</code></td><td>AI 管理中心接 Keycloak SSO</td></tr>
  </table>

  <p class="note">模板生成日期：2026-08-13 · 主机内网 IP：<code class="ph">你的内网 IP（.env 里的 SERVER_IP）</code></p>
</div>
</body>
</html>
